`timescale 1ns/1ps
//======================================================================
//  sccb_if : SCCB 버스 (관측만).
//    open-drain 이므로 tri1 (미구동 시 1, low 로 끌면 0).
//======================================================================
interface sccb_if ();
  tri1 sioc;
  tri1 siod;
endinterface
